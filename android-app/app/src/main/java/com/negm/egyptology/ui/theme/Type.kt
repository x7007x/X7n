package com.negm.egyptology.ui.theme

import androidx.compose.material3.Typography

// Uses the system font stack: bundled TTFs were removed after resource
// shrinking kept breaking Compose runtime font loading, and system fonts
// already handle both Latin and Arabic rendering.
val Typography = Typography()
